const logoutBtn=document.querySelector("#logoutBtn");function showStatusMessage(t){const o=t[new URLSearchParams(window.location.search).get("status")];null!=o&&alert(o)}null!=logoutBtn&&logoutBtn.addEventListener("click",()=>{for(let t of document.getElementsByName("logout"))t.submit()});